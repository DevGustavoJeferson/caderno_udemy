 # 💻 Comandos Básicos — Windows e Linux

- Os **comandos de terminal** são a base para interagir diretamente com o sistema operacional.  
- Mesmo com interfaces gráficas modernas, **todo programador precisa dominar o terminal**, pois ele é mais rápido, direto e usado em ambientes de desenvolvimento e servidores.
- Abaixo estão os **principais comandos básicos** de **Windows (Prompt de Comando)** e **Linux (Terminal)** — com explicações simples e práticas.

---

## 🪟 **Comandos Básicos do Windows (Prompt de Comando)**

| Comando | Função | Exemplo / Observação |
|----------|--------|----------------------|
| `dir` | Lista os arquivos e pastas do diretório atual | Similar ao `ls` no Linux |
| `cd` | Entra em um diretório | `cd Documentos` |
| `cd ..` | Volta um nível na hierarquia de pastas | Retorna para a pasta anterior |
| `cls` | Limpa a tela do terminal | Deixa o prompt limpo |
| `mkdir` | Cria uma nova pasta | `mkdir projetos` |
| `rmdir` | Remove uma pasta vazia | `rmdir testes` |
| `del` | Deleta arquivos | `del arquivo.txt` |
| `copy` | Copia arquivos | `copy origem.txt destino.txt` |
| `move` | Move ou renomeia arquivos | `move texto.txt backup\texto.txt` |
| `rename` | Renomeia um arquivo | `rename antigo.txt novo.txt` |
| `type` | Exibe o conteúdo de um arquivo de texto | `type notas.txt` |
| `echo` | Exibe mensagens ou grava texto em arquivo | `echo Olá Mundo` |
| `exit` | Fecha o prompt de comando | Encerra a sessão atual |
| `ipconfig` | Mostra as configurações de rede | Muito útil para verificar IP |
| `tasklist` | Lista os processos em execução | Como o Gerenciador de Tarefas |
| `taskkill /IM nome.exe /F` | Encerra um processo | `taskkill /IM chrome.exe /F` |

💡 **Dica:** No Windows, você pode abrir o prompt digitando **cmd** na busca do menu iniciar.

---

## 🐧 **Comandos Básicos do Linux (Terminal)**

| Comando | Função | Exemplo / Observação |
|----------|--------|----------------------|
| `ls` | Lista arquivos e pastas | `ls -l` mostra com detalhes |
| `cd` | Entra em um diretório | `cd /home/usuario` |
| `cd ..` | Volta um nível na hierarquia | Sobe uma pasta |
| `pwd` | Mostra o diretório atual | “Print Working Directory” |
| `clear` | Limpa a tela do terminal | Igual ao `cls` no Windows |
| `mkdir` | Cria uma nova pasta | `mkdir projetos` |
| `rmdir` | Remove uma pasta vazia | `rmdir testes` |
| `rm` | Remove arquivos ou pastas | `rm arquivo.txt` ou `rm -r pasta` |
| `cp` | Copia arquivos | `cp origem.txt destino.txt` |
| `mv` | Move ou renomeia arquivos | `mv antigo.txt novo.txt` |
| `cat` | Exibe o conteúdo de arquivos | `cat notas.txt` |
| `echo` | Exibe mensagens ou grava texto | `echo "Olá Mundo"` |
| `sudo` | Executa comando como administrador | `sudo apt update` |
| `apt install nome` | Instala programas (Debian/Ubuntu) | `sudo apt install git` |
| `top` | Mostra processos em tempo real | Similar ao Gerenciador de Tarefas |
| `kill PID` | Encerra um processo | `kill 1234` (PID do processo) |
| `ifconfig` | Mostra informações de rede | Pode precisar do pacote `net-tools` |

💡 **Dica:** No Linux, o terminal é aberto com `Ctrl + Alt + T`.

---

## 🧩 Comparativo Rápido

| Ação | Windows | Linux |
|------|----------|--------|
| Listar arquivos | `dir` | `ls` |
| Criar pasta | `mkdir` | `mkdir` |
| Apagar pasta | `rmdir` | `rmdir` ou `rm -r` |
| Apagar arquivo | `del` | `rm` |
| Copiar arquivo | `copy` | `cp` |
| Mover arquivo | `move` | `mv` |
| Ver conteúdo de arquivo | `type` | `cat` |
| Limpar tela | `cls` | `clear` |

---

## 📘 **Resumo Final**

Saber os comandos básicos do **Windows** e **Linux** é um **passo essencial para dominar a informática e a programação**.  
Eles permitem que você:
- Trabalhe sem depender de interface gráfica,  
- Automatize tarefas,  
- Entenda o funcionamento interno do sistema.

🔹 **Domine o terminal.** Ele é o caminho direto entre você e o computador.  
